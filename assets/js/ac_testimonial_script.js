(function () {    function setCollapsed(el) {        el.classList.add('is-collapsed');        el.classList.remove('is-expanded');        el.style.maxHeight = '0px';        el.setAttribute('aria-hidden', 'true');    }    function setExpanded(el) {        el.classList.add('is-expanded');        el.classList.remove('is-collapsed');        el.setAttribute('aria-hidden', 'false');        // set to scrollHeight to animate open        el.style.maxHeight = el.scrollHeight + 'px';        // after transition, allow it to grow naturally if content changes        var onEnd = function (e) {            if (e.propertyName !== 'max-height') return;            el.style.maxHeight = 'none';            el.removeEventListener('transitionend', onEnd);        };        el.addEventListener('transitionend', onEnd);    }    function expandWithAnimation(el) {        // if currently 'none', force numeric start point        if (getComputedStyle(el).maxHeight === 'none') {            el.style.maxHeight = el.scrollHeight + 'px';            return;        }        setExpanded(el);    }    function collapseWithAnimation(el) {        // if maxHeight is none, set a pixel value first, then collapse next frame        if (getComputedStyle(el).maxHeight === 'none') {            el.style.maxHeight = el.scrollHeight + 'px';            requestAnimationFrame(function () {                requestAnimationFrame(function () {                    setCollapsed(el);                });            });            return;        }        setCollapsed(el);    }    function init() {        var toggles = document.querySelectorAll('.c-ac-testimonial__toggle[data-target]');        if (!toggles.length) return;        toggles.forEach(function (btn) {            var targetId = btn.getAttribute('data-target');            var body = targetId ? document.getElementById(targetId) : null;            if (!body) return;            // initial state            setCollapsed(body);            btn.setAttribute('aria-expanded', 'false');            btn.textContent = 'Show more';            btn.addEventListener('click', function () {                var expanded = btn.getAttribute('aria-expanded') === 'true';                if (expanded) {                    btn.setAttribute('aria-expanded', 'false');                    btn.textContent = 'Show more';                    collapseWithAnimation(body);                } else {                    btn.setAttribute('aria-expanded', 'true');                    btn.textContent = 'Show less';                    expandWithAnimation(body);                }            });        });        // keep expanded height correct on resize (only for expanded ones)        window.addEventListener('resize', function () {            document.querySelectorAll('.c-ac-testimonial__body.is-collapsible.is-expanded').forEach(function (el) {                if (getComputedStyle(el).maxHeight !== 'none') {                    el.style.maxHeight = el.scrollHeight + 'px';                }            });        });    }    if (document.readyState === 'loading') {        document.addEventListener('DOMContentLoaded', init);    } else {        init();    }})();